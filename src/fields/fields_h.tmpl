@require(fields)
#ifndef FIELDS_H
#define FIELDS_H

#ifndef __STDC_LIMIT_MACROS
#define __STDC_LIMIT_MACROS
#endif

#include <stdint.h>
#include <string>
#include <map>
#include <sstream>

using namespace std;

namespace Fields
{

@for field in fields:
const int @{field['name']} = @{field['number']};
@end

class FieldUtils
{
    public:
        static map<int, string> createMap ();
        static string getFieldName (int f);

        static map<int, string> mFieldNames;
};

} // namespace Fields

#endif // FIELDS_H
